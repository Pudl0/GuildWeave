-- Config.lua
-- Guild-specific configuration for GuildWeave.
-- This is the ONLY file you need to edit to set up GuildWeave for your guild.
-- Currently no configuration is required for a standard installation.

GuildWeave = GuildWeave or {}
GuildWeave.Config = {}
